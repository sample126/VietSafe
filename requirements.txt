# VietSafe Core runs entirely on Python standard library (Python 3.10+)
# No external dependencies required for running the core server!

# Optional development and testing dependencies:
# pytest>=7.0.0
# python-docx>=1.1.0
